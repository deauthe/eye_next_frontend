import React from "react";
import { Skeleton } from "@/components/ui/skeleton";

const Loading = () => {
	return <div className="w-full h-full rounded-md text-8xl">Loading...</div>;
};

export default Loading;
